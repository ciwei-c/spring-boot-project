<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.c.mapper.UserMapper" >
    <resultMap id="BaseResultMap" type="com.c.model.User" >
        <id column="userId" property="userId" jdbcType="VARCHAR" />
        <result column="userName" property="userName" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List" >
        userId, userName, password
    </sql>

    <select id="getAll" resultMap="BaseResultMap"  >
        SELECT
        <include refid="Base_Column_List" />
        FROM user
    </select>

    <select id="getOne" parameterType="java.lang.String" resultMap="BaseResultMap" >
        SELECT
        <include refid="Base_Column_List" />
        FROM user
        WHERE userId = #{userId}
    </select>

    <insert id="insert" parameterType="com.c.model.User">
        INSERT INTO
        user
        (userName,password,userId)
        VALUES
        (#{userName}, #{password}, #{userId})
    </insert>

    <update id="update" parameterType="com.c.model.User" >
        UPDATE
        user
        SET
        <if test="userName != null and password == null">userName = #{userName}</if>
        <if test="userName == null and password != null">password = #{password}</if>
        <if test="userName != null and password != null">userName = #{userName}, password = #{password}</if>
        WHERE
        userId = #{userId}
    </update>

    <delete id="delete" parameterType="java.lang.String" >
        DELETE FROM
        user
        WHERE
        userId = #{userId}
    </delete>

</mapper>